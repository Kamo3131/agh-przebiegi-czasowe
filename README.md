"# agh-przebiegi-czasowe" 
To launch it, you need to clone it from github repository or download. 
1. Create build. 
2. Get inside it.
3. cmd `cmake ..`
4. cmd `cmake --build .`
To generate documentation
cmd `cmake --build . --target doc`